##############################
# THIS FILE IS AUTOGENERATED #
#    DO NOT MANUALLY EDIT    #
##############################
name: OpenVMM Release
on:
  workflow_dispatch:
    inputs:
      verbose:
        description: Run with verbose output
        default: false
        required: false
        type: boolean
jobs:
  job0:
    name: assemble openvmm vendor artifact
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: github.event.pull_request.draft == false
    steps:
    - run: |
        set -x
        i=0; while [ $i -lt 5 ] && ! sudo apt-get update; do let "i=i+1"; sleep 1; done;
        sudo apt-get -o DPkg::Lock::Timeout=60 install gcc -y
        curl --fail --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain=1.95.0 -y
        . "$HOME/.cargo/env"
        echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
        rustup show
      if: runner.os == 'Linux'
      name: rustup (Linux)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/x86_64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'X64'
      name: rustup (Windows X64)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/aarch64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'ARM64'
      name: rustup (Windows ARM64)
      shell: bash
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        path: flowey_bootstrap
    - name: Build flowey
      run: |
        set -x
        CARGO_INCREMENTAL=0 cargo build -p flowey_hvlite --target x86_64-unknown-linux-gnu --profile flowey-ci
        OutDirNormal=$(echo "${{ runner.temp }}/bootstrapped-flowey" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        mkdir -p "$OutDirNormal"
        mv ./.github/workflows/openvmm-release.yaml "$OutDirNormal/pipeline.yaml"
        mv target/x86_64-unknown-linux-gnu/flowey-ci/flowey_hvlite "$OutDirNormal/flowey"
      working-directory: flowey_bootstrap
      shell: bash
    - run: echo "${{ runner.temp }}/bootstrapped-flowey" >> $GITHUB_PATH
      shell: bash
      name: 🌼📦 Add flowey to PATH
    - name: 🌼🔎 Self-check YAML
      run: |-
        ESCAPED_AGENT_TEMPDIR=$(
        cat <<'EOF' | sed 's/\\/\\\\/g'
        ${{ runner.temp }}
        EOF
        )
        flowey pipeline github --runtime $ESCAPED_AGENT_TEMPDIR/bootstrapped-flowey/pipeline.yaml --out .github/workflows/openvmm-release.yaml ci openvmm-release
      shell: bash
    - name: 🌼🛫 Initialize job
      run: |
        AgentTempDirNormal="${{ runner.temp }}"
        AgentTempDirNormal=$(echo "$AgentTempDirNormal" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        echo "AgentTempDirNormal=$AgentTempDirNormal" >> $GITHUB_ENV

        chmod +x $AgentTempDirNormal/bootstrapped-flowey/flowey

        echo '"debug"' | flowey v 0 'FLOWEY_LOG' update
        echo "${{ runner.temp }}/work" | flowey v 0 '_internal_WORKING_DIR' --is-raw-string update

        cat <<'EOF' | flowey v 0 'verbose' update
        ${{ inputs.verbose != '' && inputs.verbose || 'false' }}
        EOF
        mkdir -p "$AgentTempDirNormal/publish_artifacts/openvmm-vendor-release"
        echo "$AgentTempDirNormal/publish_artifacts/openvmm-vendor-release" | flowey v 0 'artifact_publish_from_openvmm-vendor-release' --is-raw-string update
      shell: bash
    - name: check if openvmm needs to be cloned
      run: |-
        flowey e 0 flowey_lib_common::git_checkout 0
        flowey v 0 'flowey_lib_common::git_checkout:0:flowey_lib_common/src/git_checkout.rs' --is-raw-string --condvar flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs write-to-env github floweyvar1
        flowey v 0 'flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs' write-to-env github FLOWEY_CONDITION
      shell: bash
    - id: flowey_lib_common__git_checkout__1
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        fetch-depth: '1'
        path: repo0
        persist-credentials: ${{ env.floweyvar1 }}
      name: checkout repo openvmm
      if: ${{ fromJSON(env.FLOWEY_CONDITION) }}
    - name: report cloned repo directories
      run: |-
        flowey v 0 'flowey_lib_common::git_checkout:4:flowey_core/src/node/github_context.rs' --is-raw-string update --env-source github.workspace <<EOF
        ${{ github.workspace }}
        EOF
        flowey e 0 flowey_lib_common::git_checkout 3
      shell: bash
    - name: print system info
      run: |-
        flowey e 0 flowey_lib_common::system_info 0
        flowey e 0 flowey_lib_hvlite::git_checkout_openvmm_repo 0
      shell: bash
    - name: add default cargo home to path
      run: flowey e 0 flowey_lib_common::install_rust 0
      shell: bash
    - name: install Rust
      run: flowey e 0 flowey_lib_common::install_rust 1
      shell: bash
    - name: detect active toolchain
      run: flowey e 0 flowey_lib_common::install_rust 2
      shell: bash
    - name: assemble OpenVMM vendor archive
      run: |-
        flowey e 0 flowey_lib_hvlite::assemble_openvmm_vendor_release 0
        flowey e 0 flowey_core::pipeline::artifact::publish 0
      shell: bash
    - name: 🌼📦 Publish openvmm-vendor-release
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: openvmm-vendor-release
        path: ${{ runner.temp }}/publish_artifacts/openvmm-vendor-release/
        include-hidden-files: true
    - name: 🌼🧼 Redact bootstrap var db
      run: rm $AgentTempDirNormal/bootstrapped-flowey/job0.json
      shell: bash
    - name: 🌼🥾 Publish bootstrapped flowey
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: _internal-flowey-bootstrap-x86_64-linux-uid-5
        path: ${{ runner.temp }}/bootstrapped-flowey
  job1:
    name: validate openvmm distribution build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs:
    - job0
    if: github.event.pull_request.draft == false
    steps:
    - name: 🌼📦 Download artifacts
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with:
        pattern: '{_internal-flowey-bootstrap-x86_64-linux-uid-5,openvmm-vendor-release}'
        path: ${{ runner.temp }}/used_artifacts/
    - run: echo "${{ runner.temp }}/used_artifacts/_internal-flowey-bootstrap-x86_64-linux-uid-5" >> $GITHUB_PATH
      shell: bash
      name: 🌼📦 Add flowey to PATH
    - name: 🌼🛫 Initialize job
      run: |
        AgentTempDirNormal="${{ runner.temp }}"
        AgentTempDirNormal=$(echo "$AgentTempDirNormal" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        echo "AgentTempDirNormal=$AgentTempDirNormal" >> $GITHUB_ENV

        chmod +x $AgentTempDirNormal/used_artifacts/_internal-flowey-bootstrap-x86_64-linux-uid-5/flowey

        echo '"debug"' | flowey v 1 'FLOWEY_LOG' update
        echo "${{ runner.temp }}/work" | flowey v 1 '_internal_WORKING_DIR' --is-raw-string update

        cat <<'EOF' | flowey v 1 'verbose' update
        ${{ inputs.verbose != '' && inputs.verbose || 'false' }}
        EOF
        echo "$AgentTempDirNormal/used_artifacts/openvmm-vendor-release" | flowey v 1 'artifact_use_from_openvmm-vendor-release' --is-raw-string update
      shell: bash
    - name: check if openvmm needs to be cloned
      run: |-
        flowey e 1 flowey_core::pipeline::artifact::resolve 0
        flowey e 1 flowey_lib_common::git_checkout 0
        flowey v 1 'flowey_lib_common::git_checkout:0:flowey_lib_common/src/git_checkout.rs' --is-raw-string --condvar flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs write-to-env github floweyvar1
        flowey v 1 'flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs' write-to-env github FLOWEY_CONDITION
      shell: bash
    - id: flowey_lib_common__git_checkout__1
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        fetch-depth: '1'
        path: repo0
        persist-credentials: ${{ env.floweyvar1 }}
      name: checkout repo openvmm
      if: ${{ fromJSON(env.FLOWEY_CONDITION) }}
    - name: report cloned repo directories
      run: |-
        flowey v 1 'flowey_lib_common::git_checkout:4:flowey_core/src/node/github_context.rs' --is-raw-string update --env-source github.workspace <<EOF
        ${{ github.workspace }}
        EOF
        flowey e 1 flowey_lib_common::git_checkout 3
      shell: bash
    - name: print system info
      run: |-
        flowey e 1 flowey_lib_common::system_info 0
        flowey e 1 flowey_lib_hvlite::git_checkout_openvmm_repo 0
      shell: bash
    - name: add default cargo home to path
      run: flowey e 1 flowey_lib_common::install_rust 0
      shell: bash
    - name: install Rust
      run: flowey e 1 flowey_lib_common::install_rust 1
      shell: bash
    - name: detect active toolchain
      run: flowey e 1 flowey_lib_common::install_rust 2
      shell: bash
    - name: checking if packages need to be installed
      run: flowey e 1 flowey_lib_common::install_dist_pkg 0
      shell: bash
    - name: installing packages
      run: flowey e 1 flowey_lib_common::install_dist_pkg 1
      shell: bash
    - name: build openvmm in a distribution configuration
      run: flowey e 1 flowey_lib_hvlite::_jobs::check_distro_build 0
      shell: bash
  job2:
    name: build openvmm [x86_64-unknown-linux-musl]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: github.event.pull_request.draft == false
    steps:
    - run: |
        set -x
        i=0; while [ $i -lt 5 ] && ! sudo apt-get update; do let "i=i+1"; sleep 1; done;
        sudo apt-get -o DPkg::Lock::Timeout=60 install gcc -y
        curl --fail --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain=1.95.0 -y
        . "$HOME/.cargo/env"
        echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
        rustup show
      if: runner.os == 'Linux'
      name: rustup (Linux)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/x86_64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'X64'
      name: rustup (Windows X64)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/aarch64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'ARM64'
      name: rustup (Windows ARM64)
      shell: bash
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        path: flowey_bootstrap
    - name: Build flowey
      run: |
        set -x
        CARGO_INCREMENTAL=0 cargo build -p flowey_hvlite --target x86_64-unknown-linux-gnu --profile flowey-ci
        OutDirNormal=$(echo "${{ runner.temp }}/bootstrapped-flowey" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        mkdir -p "$OutDirNormal"
        mv ./.github/workflows/openvmm-release.yaml "$OutDirNormal/pipeline.yaml"
        mv target/x86_64-unknown-linux-gnu/flowey-ci/flowey_hvlite "$OutDirNormal/flowey"
      working-directory: flowey_bootstrap
      shell: bash
    - run: echo "${{ runner.temp }}/bootstrapped-flowey" >> $GITHUB_PATH
      shell: bash
      name: 🌼📦 Add flowey to PATH
    - name: 🌼🔎 Self-check YAML
      run: |-
        ESCAPED_AGENT_TEMPDIR=$(
        cat <<'EOF' | sed 's/\\/\\\\/g'
        ${{ runner.temp }}
        EOF
        )
        flowey pipeline github --runtime $ESCAPED_AGENT_TEMPDIR/bootstrapped-flowey/pipeline.yaml --out .github/workflows/openvmm-release.yaml ci openvmm-release
      shell: bash
    - name: 🌼🛫 Initialize job
      run: |
        AgentTempDirNormal="${{ runner.temp }}"
        AgentTempDirNormal=$(echo "$AgentTempDirNormal" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        echo "AgentTempDirNormal=$AgentTempDirNormal" >> $GITHUB_ENV

        chmod +x $AgentTempDirNormal/bootstrapped-flowey/flowey

        echo '"debug"' | flowey v 2 'FLOWEY_LOG' update
        echo "${{ runner.temp }}/work" | flowey v 2 '_internal_WORKING_DIR' --is-raw-string update

        cat <<'EOF' | flowey v 2 'verbose' update
        ${{ inputs.verbose != '' && inputs.verbose || 'false' }}
        EOF
        mkdir -p "$AgentTempDirNormal/publish_artifacts/openvmm-x86_64-unknown-linux-musl"
        echo "$AgentTempDirNormal/publish_artifacts/openvmm-x86_64-unknown-linux-musl" | flowey v 2 'artifact_publish_from_openvmm-x86_64-unknown-linux-musl' --is-raw-string update
      shell: bash
    - name: add default cargo home to path
      run: flowey e 2 flowey_lib_common::install_rust 0
      shell: bash
    - name: install Rust
      run: flowey e 2 flowey_lib_common::install_rust 1
      shell: bash
    - name: detect active toolchain
      run: |-
        flowey e 2 flowey_lib_common::install_rust 2
        flowey e 2 flowey_lib_common::cfg_cargo_common_flags 0
      shell: bash
    - name: checking if packages need to be installed
      run: flowey e 2 flowey_lib_common::install_dist_pkg 0
      shell: bash
    - name: installing packages
      run: flowey e 2 flowey_lib_common::install_dist_pkg 1
      shell: bash
    - name: check if openvmm needs to be cloned
      run: |-
        flowey e 2 flowey_lib_common::git_checkout 0
        flowey v 2 'flowey_lib_common::git_checkout:0:flowey_lib_common/src/git_checkout.rs' --is-raw-string --condvar flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs write-to-env github floweyvar1
        flowey v 2 'flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs' write-to-env github FLOWEY_CONDITION
      shell: bash
    - id: flowey_lib_common__git_checkout__1
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        fetch-depth: '1'
        path: repo0
        persist-credentials: ${{ env.floweyvar1 }}
      name: checkout repo openvmm
      if: ${{ fromJSON(env.FLOWEY_CONDITION) }}
    - name: report cloned repo directories
      run: |-
        flowey v 2 'flowey_lib_common::git_checkout:4:flowey_core/src/node/github_context.rs' --is-raw-string update --env-source github.workspace <<EOF
        ${{ github.workspace }}
        EOF
        flowey e 2 flowey_lib_common::git_checkout 3
      shell: bash
    - name: print system info
      run: |-
        flowey e 2 flowey_lib_common::system_info 0
        flowey e 2 flowey_lib_hvlite::git_checkout_openvmm_repo 0
      shell: bash
    - name: set '-Dwarnings' in .cargo/config.toml
      run: flowey e 2 flowey_lib_hvlite::init_openvmm_cargo_config_deny_warnings 0
      shell: bash
    - name: create gh-release-download cache dir
      run: flowey e 2 flowey_lib_common::download_gh_release 0
      shell: bash
    - name: Pre-processing cache vars
      run: |-
        flowey e 2 flowey_lib_common::cache 0
        flowey v 2 'flowey_lib_common::cache:2:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar2
        flowey v 2 'flowey_lib_common::cache:1:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar3
      shell: bash
    - id: flowey_lib_common__cache__1
      uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
      with:
        key: ${{ env.floweyvar2 }}
        path: ${{ env.floweyvar3 }}
      name: 'Restore cache: gh-release-download'
    - name: download artifacts from github releases
      run: |-
        flowey v 2 'flowey_lib_common::cache:4:flowey_lib_common/src/cache.rs' --is-raw-string update --env-source steps.flowey_lib_common__cache__1.outputs.cache-hit <<EOF
        ${{ steps.flowey_lib_common__cache__1.outputs.cache-hit }}
        EOF
        flowey e 2 flowey_lib_common::cache 2
        flowey e 2 flowey_lib_common::download_gh_release 1
      shell: bash
    - name: unpack protoc
      run: |-
        flowey e 2 flowey_lib_common::resolve_protoc 0
        flowey e 2 flowey_lib_hvlite::cfg_openvmm_magicpath 0
      shell: bash
    - name: symlink protoc
      run: flowey e 2 flowey_lib_hvlite::init_openvmm_magicpath_protoc 0
      shell: bash
    - name: unpack openvmm-deps archive
      run: flowey e 2 flowey_lib_hvlite::resolve_openvmm_deps 0
      shell: bash
    - name: extract X86_64 sysroot.tar.gz
      run: |-
        flowey e 2 flowey_lib_hvlite::init_openvmm_magicpath_openhcl_sysroot 0
        flowey e 2 flowey_lib_hvlite::init_cross_build 0
      shell: bash
    - name: cargo build openvmm
      run: |-
        flowey e 2 flowey_lib_common::run_cargo_build 0
        flowey e 2 flowey_lib_hvlite::run_cargo_build 0
      shell: bash
    - name: split debug symbols
      run: |-
        flowey e 2 flowey_lib_hvlite::run_split_debug_info 0
        flowey e 2 flowey_lib_hvlite::run_cargo_build 1
        flowey e 2 flowey_lib_hvlite::build_openvmm 0
        flowey e 2 flowey_core::pipeline::artifact::publish 0
      shell: bash
    - name: 'validate cache entry: gh-release-download'
      run: flowey e 2 flowey_lib_common::cache 3
      shell: bash
    - name: 🌼📦 Publish openvmm-x86_64-unknown-linux-musl
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: openvmm-x86_64-unknown-linux-musl
        path: ${{ runner.temp }}/publish_artifacts/openvmm-x86_64-unknown-linux-musl/
        include-hidden-files: true
  job3:
    name: build openvmm [aarch64-unknown-linux-musl]
    runs-on: ubuntu-latest
    permissions:
      contents: read
    if: github.event.pull_request.draft == false
    steps:
    - run: |
        set -x
        i=0; while [ $i -lt 5 ] && ! sudo apt-get update; do let "i=i+1"; sleep 1; done;
        sudo apt-get -o DPkg::Lock::Timeout=60 install gcc -y
        curl --fail --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain=1.95.0 -y
        . "$HOME/.cargo/env"
        echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
        rustup show
      if: runner.os == 'Linux'
      name: rustup (Linux)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/x86_64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'X64'
      name: rustup (Windows X64)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/aarch64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'ARM64'
      name: rustup (Windows ARM64)
      shell: bash
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        path: flowey_bootstrap
    - name: Build flowey
      run: |
        set -x
        CARGO_INCREMENTAL=0 cargo build -p flowey_hvlite --target x86_64-unknown-linux-gnu --profile flowey-ci
        OutDirNormal=$(echo "${{ runner.temp }}/bootstrapped-flowey" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        mkdir -p "$OutDirNormal"
        mv ./.github/workflows/openvmm-release.yaml "$OutDirNormal/pipeline.yaml"
        mv target/x86_64-unknown-linux-gnu/flowey-ci/flowey_hvlite "$OutDirNormal/flowey"
      working-directory: flowey_bootstrap
      shell: bash
    - run: echo "${{ runner.temp }}/bootstrapped-flowey" >> $GITHUB_PATH
      shell: bash
      name: 🌼📦 Add flowey to PATH
    - name: 🌼🔎 Self-check YAML
      run: |-
        ESCAPED_AGENT_TEMPDIR=$(
        cat <<'EOF' | sed 's/\\/\\\\/g'
        ${{ runner.temp }}
        EOF
        )
        flowey pipeline github --runtime $ESCAPED_AGENT_TEMPDIR/bootstrapped-flowey/pipeline.yaml --out .github/workflows/openvmm-release.yaml ci openvmm-release
      shell: bash
    - name: 🌼🛫 Initialize job
      run: |
        AgentTempDirNormal="${{ runner.temp }}"
        AgentTempDirNormal=$(echo "$AgentTempDirNormal" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        echo "AgentTempDirNormal=$AgentTempDirNormal" >> $GITHUB_ENV

        chmod +x $AgentTempDirNormal/bootstrapped-flowey/flowey

        echo '"debug"' | flowey v 3 'FLOWEY_LOG' update
        echo "${{ runner.temp }}/work" | flowey v 3 '_internal_WORKING_DIR' --is-raw-string update

        cat <<'EOF' | flowey v 3 'verbose' update
        ${{ inputs.verbose != '' && inputs.verbose || 'false' }}
        EOF
        mkdir -p "$AgentTempDirNormal/publish_artifacts/openvmm-aarch64-unknown-linux-musl"
        echo "$AgentTempDirNormal/publish_artifacts/openvmm-aarch64-unknown-linux-musl" | flowey v 3 'artifact_publish_from_openvmm-aarch64-unknown-linux-musl' --is-raw-string update
      shell: bash
    - name: add default cargo home to path
      run: flowey e 3 flowey_lib_common::install_rust 0
      shell: bash
    - name: install Rust
      run: flowey e 3 flowey_lib_common::install_rust 1
      shell: bash
    - name: detect active toolchain
      run: |-
        flowey e 3 flowey_lib_common::install_rust 2
        flowey e 3 flowey_lib_common::cfg_cargo_common_flags 0
      shell: bash
    - name: checking if packages need to be installed
      run: flowey e 3 flowey_lib_common::install_dist_pkg 0
      shell: bash
    - name: installing packages
      run: flowey e 3 flowey_lib_common::install_dist_pkg 1
      shell: bash
    - name: check if openvmm needs to be cloned
      run: |-
        flowey e 3 flowey_lib_common::git_checkout 0
        flowey v 3 'flowey_lib_common::git_checkout:0:flowey_lib_common/src/git_checkout.rs' --is-raw-string --condvar flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs write-to-env github floweyvar1
        flowey v 3 'flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs' write-to-env github FLOWEY_CONDITION
      shell: bash
    - id: flowey_lib_common__git_checkout__1
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        fetch-depth: '1'
        path: repo0
        persist-credentials: ${{ env.floweyvar1 }}
      name: checkout repo openvmm
      if: ${{ fromJSON(env.FLOWEY_CONDITION) }}
    - name: report cloned repo directories
      run: |-
        flowey v 3 'flowey_lib_common::git_checkout:4:flowey_core/src/node/github_context.rs' --is-raw-string update --env-source github.workspace <<EOF
        ${{ github.workspace }}
        EOF
        flowey e 3 flowey_lib_common::git_checkout 3
      shell: bash
    - name: print system info
      run: |-
        flowey e 3 flowey_lib_common::system_info 0
        flowey e 3 flowey_lib_hvlite::git_checkout_openvmm_repo 0
      shell: bash
    - name: set '-Dwarnings' in .cargo/config.toml
      run: flowey e 3 flowey_lib_hvlite::init_openvmm_cargo_config_deny_warnings 0
      shell: bash
    - name: create gh-release-download cache dir
      run: flowey e 3 flowey_lib_common::download_gh_release 0
      shell: bash
    - name: Pre-processing cache vars
      run: |-
        flowey e 3 flowey_lib_common::cache 0
        flowey v 3 'flowey_lib_common::cache:2:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar2
        flowey v 3 'flowey_lib_common::cache:1:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar3
      shell: bash
    - id: flowey_lib_common__cache__1
      uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
      with:
        key: ${{ env.floweyvar2 }}
        path: ${{ env.floweyvar3 }}
      name: 'Restore cache: gh-release-download'
    - name: download artifacts from github releases
      run: |-
        flowey v 3 'flowey_lib_common::cache:4:flowey_lib_common/src/cache.rs' --is-raw-string update --env-source steps.flowey_lib_common__cache__1.outputs.cache-hit <<EOF
        ${{ steps.flowey_lib_common__cache__1.outputs.cache-hit }}
        EOF
        flowey e 3 flowey_lib_common::cache 2
        flowey e 3 flowey_lib_common::download_gh_release 1
      shell: bash
    - name: unpack protoc
      run: |-
        flowey e 3 flowey_lib_common::resolve_protoc 0
        flowey e 3 flowey_lib_hvlite::cfg_openvmm_magicpath 0
      shell: bash
    - name: symlink protoc
      run: flowey e 3 flowey_lib_hvlite::init_openvmm_magicpath_protoc 0
      shell: bash
    - name: unpack openvmm-deps archive
      run: flowey e 3 flowey_lib_hvlite::resolve_openvmm_deps 0
      shell: bash
    - name: extract Aarch64 sysroot.tar.gz
      run: |-
        flowey e 3 flowey_lib_hvlite::init_openvmm_magicpath_openhcl_sysroot 0
        flowey e 3 flowey_lib_hvlite::init_cross_build 0
      shell: bash
    - name: cargo build openvmm
      run: |-
        flowey e 3 flowey_lib_common::run_cargo_build 0
        flowey e 3 flowey_lib_hvlite::run_cargo_build 0
      shell: bash
    - name: split debug symbols
      run: |-
        flowey e 3 flowey_lib_hvlite::run_split_debug_info 0
        flowey e 3 flowey_lib_hvlite::run_cargo_build 1
        flowey e 3 flowey_lib_hvlite::build_openvmm 0
        flowey e 3 flowey_core::pipeline::artifact::publish 0
      shell: bash
    - name: 'validate cache entry: gh-release-download'
      run: flowey e 3 flowey_lib_common::cache 3
      shell: bash
    - name: 🌼📦 Publish openvmm-aarch64-unknown-linux-musl
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: openvmm-aarch64-unknown-linux-musl
        path: ${{ runner.temp }}/publish_artifacts/openvmm-aarch64-unknown-linux-musl/
        include-hidden-files: true
  job4:
    name: build unsigned openvmm [x86_64-pc-windows-msvc]
    runs-on: windows-latest
    permissions:
      contents: read
    if: github.event.pull_request.draft == false
    steps:
    - run: |
        set -x
        i=0; while [ $i -lt 5 ] && ! sudo apt-get update; do let "i=i+1"; sleep 1; done;
        sudo apt-get -o DPkg::Lock::Timeout=60 install gcc -y
        curl --fail --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain=1.95.0 -y
        . "$HOME/.cargo/env"
        echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
        rustup show
      if: runner.os == 'Linux'
      name: rustup (Linux)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/x86_64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'X64'
      name: rustup (Windows X64)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/aarch64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'ARM64'
      name: rustup (Windows ARM64)
      shell: bash
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        path: flowey_bootstrap
    - name: Build flowey
      run: |
        set -x
        CARGO_INCREMENTAL=0 cargo build -p flowey_hvlite --target x86_64-pc-windows-msvc --profile flowey-ci
        OutDirNormal=$(echo "${{ runner.temp }}/bootstrapped-flowey" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        mkdir -p "$OutDirNormal"
        mv ./.github/workflows/openvmm-release.yaml "$OutDirNormal/pipeline.yaml"
        mv target/x86_64-pc-windows-msvc/flowey-ci/flowey_hvlite.exe "$OutDirNormal/flowey.exe"
      working-directory: flowey_bootstrap
      shell: bash
    - run: echo "${{ runner.temp }}/bootstrapped-flowey" >> $GITHUB_PATH
      shell: bash
      name: 🌼📦 Add flowey to PATH
    - name: 🌼🔎 Self-check YAML
      run: |-
        ESCAPED_AGENT_TEMPDIR=$(
        cat <<'EOF' | sed 's/\\/\\\\/g'
        ${{ runner.temp }}
        EOF
        )
        flowey.exe pipeline github --runtime $ESCAPED_AGENT_TEMPDIR\\bootstrapped-flowey\\pipeline.yaml --out .github/workflows/openvmm-release.yaml ci openvmm-release
      shell: bash
    - name: 🌼🛫 Initialize job
      run: |
        AgentTempDirNormal="${{ runner.temp }}"
        AgentTempDirNormal=$(echo "$AgentTempDirNormal" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        echo "AgentTempDirNormal=$AgentTempDirNormal" >> $GITHUB_ENV

        chmod +x $AgentTempDirNormal/bootstrapped-flowey/flowey.exe

        echo '"debug"' | flowey.exe v 4 'FLOWEY_LOG' update
        echo "${{ runner.temp }}/work" | flowey.exe v 4 '_internal_WORKING_DIR' --is-raw-string update

        cat <<'EOF' | flowey.exe v 4 'verbose' update
        ${{ inputs.verbose != '' && inputs.verbose || 'false' }}
        EOF
        mkdir -p "$AgentTempDirNormal/publish_artifacts/openvmm-x86_64-pc-windows-msvc-unsigned"
        echo "${{ runner.temp }}\\publish_artifacts\\openvmm-x86_64-pc-windows-msvc-unsigned" | flowey.exe v 4 'artifact_publish_from_openvmm-x86_64-pc-windows-msvc-unsigned' --is-raw-string update
      shell: bash
    - name: add default cargo home to path
      run: flowey.exe e 4 flowey_lib_common::install_rust 0
      shell: bash
    - name: install Rust
      run: flowey.exe e 4 flowey_lib_common::install_rust 1
      shell: bash
    - name: detect active toolchain
      run: |-
        flowey.exe e 4 flowey_lib_common::install_rust 2
        flowey.exe e 4 flowey_lib_common::cfg_cargo_common_flags 0
      shell: bash
    - name: check if openvmm needs to be cloned
      run: |-
        flowey.exe e 4 flowey_lib_common::git_checkout 0
        flowey.exe v 4 'flowey_lib_common::git_checkout:0:flowey_lib_common/src/git_checkout.rs' --is-raw-string --condvar flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs write-to-env github floweyvar1
        flowey.exe v 4 'flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs' write-to-env github FLOWEY_CONDITION
      shell: bash
    - id: flowey_lib_common__git_checkout__1
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        fetch-depth: '1'
        path: repo0
        persist-credentials: ${{ env.floweyvar1 }}
      name: checkout repo openvmm
      if: ${{ fromJSON(env.FLOWEY_CONDITION) }}
    - name: report cloned repo directories
      run: |-
        flowey.exe v 4 'flowey_lib_common::git_checkout:4:flowey_core/src/node/github_context.rs' --is-raw-string update --env-source github.workspace <<EOF
        ${{ github.workspace }}
        EOF
        flowey.exe e 4 flowey_lib_common::git_checkout 3
      shell: bash
    - name: print system info
      run: |-
        flowey.exe e 4 flowey_lib_common::system_info 0
        flowey.exe e 4 flowey_lib_hvlite::git_checkout_openvmm_repo 0
      shell: bash
    - name: set '-Dwarnings' in .cargo/config.toml
      run: flowey.exe e 4 flowey_lib_hvlite::init_openvmm_cargo_config_deny_warnings 0
      shell: bash
    - name: create gh-release-download cache dir
      run: flowey.exe e 4 flowey_lib_common::download_gh_release 0
      shell: bash
    - name: Pre-processing cache vars
      run: |-
        flowey.exe e 4 flowey_lib_common::cache 0
        flowey.exe v 4 'flowey_lib_common::cache:2:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar2
        flowey.exe v 4 'flowey_lib_common::cache:1:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar3
      shell: bash
    - id: flowey_lib_common__cache__1
      uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
      with:
        key: ${{ env.floweyvar2 }}
        path: ${{ env.floweyvar3 }}
      name: 'Restore cache: gh-release-download'
    - name: download artifacts from github releases
      run: |-
        flowey.exe v 4 'flowey_lib_common::cache:4:flowey_lib_common/src/cache.rs' --is-raw-string update --env-source steps.flowey_lib_common__cache__1.outputs.cache-hit <<EOF
        ${{ steps.flowey_lib_common__cache__1.outputs.cache-hit }}
        EOF
        flowey.exe e 4 flowey_lib_common::cache 2
        flowey.exe e 4 flowey_lib_common::download_gh_release 1
      shell: bash
    - name: unpack protoc
      run: |-
        flowey.exe e 4 flowey_lib_common::resolve_protoc 0
        flowey.exe e 4 flowey_lib_hvlite::cfg_openvmm_magicpath 0
      shell: bash
    - name: symlink protoc
      run: |-
        flowey.exe e 4 flowey_lib_hvlite::init_openvmm_magicpath_protoc 0
        flowey.exe e 4 flowey_lib_hvlite::init_cross_build 0
      shell: bash
    - name: cargo build openvmm
      run: |-
        flowey.exe e 4 flowey_lib_common::run_cargo_build 0
        flowey.exe e 4 flowey_lib_hvlite::run_cargo_build 0
        flowey.exe e 4 flowey_lib_hvlite::build_openvmm 0
        flowey.exe e 4 flowey_core::pipeline::artifact::publish 0
      shell: bash
    - name: 'validate cache entry: gh-release-download'
      run: flowey.exe e 4 flowey_lib_common::cache 3
      shell: bash
    - name: 🌼📦 Publish openvmm-x86_64-pc-windows-msvc-unsigned
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: openvmm-x86_64-pc-windows-msvc-unsigned
        path: ${{ runner.temp }}/publish_artifacts/openvmm-x86_64-pc-windows-msvc-unsigned/
        include-hidden-files: true
  job5:
    name: build unsigned openvmm [aarch64-pc-windows-msvc]
    runs-on: windows-latest
    permissions:
      contents: read
    if: github.event.pull_request.draft == false
    steps:
    - run: |
        set -x
        i=0; while [ $i -lt 5 ] && ! sudo apt-get update; do let "i=i+1"; sleep 1; done;
        sudo apt-get -o DPkg::Lock::Timeout=60 install gcc -y
        curl --fail --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- --default-toolchain=1.95.0 -y
        . "$HOME/.cargo/env"
        echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
        rustup show
      if: runner.os == 'Linux'
      name: rustup (Linux)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/x86_64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'X64'
      name: rustup (Windows X64)
      shell: bash
    - run: |
        set -x
        curl --fail -sSfLo rustup-init.exe https://win.rustup.rs/aarch64 --output rustup-init
        ./rustup-init.exe -y --default-toolchain=1.95.0
        echo "$USERPROFILE\\.cargo\\bin" >> $GITHUB_PATH
      if: runner.os == 'Windows' && runner.arch == 'ARM64'
      name: rustup (Windows ARM64)
      shell: bash
    - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        path: flowey_bootstrap
    - name: Build flowey
      run: |
        set -x
        CARGO_INCREMENTAL=0 cargo build -p flowey_hvlite --target x86_64-pc-windows-msvc --profile flowey-ci
        OutDirNormal=$(echo "${{ runner.temp }}/bootstrapped-flowey" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        mkdir -p "$OutDirNormal"
        mv ./.github/workflows/openvmm-release.yaml "$OutDirNormal/pipeline.yaml"
        mv target/x86_64-pc-windows-msvc/flowey-ci/flowey_hvlite.exe "$OutDirNormal/flowey.exe"
      working-directory: flowey_bootstrap
      shell: bash
    - run: echo "${{ runner.temp }}/bootstrapped-flowey" >> $GITHUB_PATH
      shell: bash
      name: 🌼📦 Add flowey to PATH
    - name: 🌼🔎 Self-check YAML
      run: |-
        ESCAPED_AGENT_TEMPDIR=$(
        cat <<'EOF' | sed 's/\\/\\\\/g'
        ${{ runner.temp }}
        EOF
        )
        flowey.exe pipeline github --runtime $ESCAPED_AGENT_TEMPDIR\\bootstrapped-flowey\\pipeline.yaml --out .github/workflows/openvmm-release.yaml ci openvmm-release
      shell: bash
    - name: 🌼🛫 Initialize job
      run: |
        AgentTempDirNormal="${{ runner.temp }}"
        AgentTempDirNormal=$(echo "$AgentTempDirNormal" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        echo "AgentTempDirNormal=$AgentTempDirNormal" >> $GITHUB_ENV

        chmod +x $AgentTempDirNormal/bootstrapped-flowey/flowey.exe

        echo '"debug"' | flowey.exe v 5 'FLOWEY_LOG' update
        echo "${{ runner.temp }}/work" | flowey.exe v 5 '_internal_WORKING_DIR' --is-raw-string update

        cat <<'EOF' | flowey.exe v 5 'verbose' update
        ${{ inputs.verbose != '' && inputs.verbose || 'false' }}
        EOF
        mkdir -p "$AgentTempDirNormal/publish_artifacts/openvmm-aarch64-pc-windows-msvc-unsigned"
        echo "${{ runner.temp }}\\publish_artifacts\\openvmm-aarch64-pc-windows-msvc-unsigned" | flowey.exe v 5 'artifact_publish_from_openvmm-aarch64-pc-windows-msvc-unsigned' --is-raw-string update
      shell: bash
    - name: add default cargo home to path
      run: flowey.exe e 5 flowey_lib_common::install_rust 0
      shell: bash
    - name: install Rust
      run: flowey.exe e 5 flowey_lib_common::install_rust 1
      shell: bash
    - name: detect active toolchain
      run: |-
        flowey.exe e 5 flowey_lib_common::install_rust 2
        flowey.exe e 5 flowey_lib_common::cfg_cargo_common_flags 0
      shell: bash
    - name: check if openvmm needs to be cloned
      run: |-
        flowey.exe e 5 flowey_lib_common::git_checkout 0
        flowey.exe v 5 'flowey_lib_common::git_checkout:0:flowey_lib_common/src/git_checkout.rs' --is-raw-string --condvar flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs write-to-env github floweyvar1
        flowey.exe v 5 'flowey_lib_common::git_checkout:1:flowey_lib_common/src/git_checkout.rs' write-to-env github FLOWEY_CONDITION
      shell: bash
    - id: flowey_lib_common__git_checkout__1
      uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
      with:
        fetch-depth: '1'
        path: repo0
        persist-credentials: ${{ env.floweyvar1 }}
      name: checkout repo openvmm
      if: ${{ fromJSON(env.FLOWEY_CONDITION) }}
    - name: report cloned repo directories
      run: |-
        flowey.exe v 5 'flowey_lib_common::git_checkout:4:flowey_core/src/node/github_context.rs' --is-raw-string update --env-source github.workspace <<EOF
        ${{ github.workspace }}
        EOF
        flowey.exe e 5 flowey_lib_common::git_checkout 3
      shell: bash
    - name: print system info
      run: |-
        flowey.exe e 5 flowey_lib_common::system_info 0
        flowey.exe e 5 flowey_lib_hvlite::git_checkout_openvmm_repo 0
      shell: bash
    - name: set '-Dwarnings' in .cargo/config.toml
      run: flowey.exe e 5 flowey_lib_hvlite::init_openvmm_cargo_config_deny_warnings 0
      shell: bash
    - name: create gh-release-download cache dir
      run: flowey.exe e 5 flowey_lib_common::download_gh_release 0
      shell: bash
    - name: Pre-processing cache vars
      run: |-
        flowey.exe e 5 flowey_lib_common::cache 0
        flowey.exe v 5 'flowey_lib_common::cache:2:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar2
        flowey.exe v 5 'flowey_lib_common::cache:1:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar3
      shell: bash
    - id: flowey_lib_common__cache__1
      uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
      with:
        key: ${{ env.floweyvar2 }}
        path: ${{ env.floweyvar3 }}
      name: 'Restore cache: gh-release-download'
    - name: download artifacts from github releases
      run: |-
        flowey.exe v 5 'flowey_lib_common::cache:4:flowey_lib_common/src/cache.rs' --is-raw-string update --env-source steps.flowey_lib_common__cache__1.outputs.cache-hit <<EOF
        ${{ steps.flowey_lib_common__cache__1.outputs.cache-hit }}
        EOF
        flowey.exe e 5 flowey_lib_common::cache 2
        flowey.exe e 5 flowey_lib_common::download_gh_release 1
      shell: bash
    - name: unpack protoc
      run: |-
        flowey.exe e 5 flowey_lib_common::resolve_protoc 0
        flowey.exe e 5 flowey_lib_hvlite::cfg_openvmm_magicpath 0
      shell: bash
    - name: symlink protoc
      run: |-
        flowey.exe e 5 flowey_lib_hvlite::init_openvmm_magicpath_protoc 0
        flowey.exe e 5 flowey_lib_hvlite::init_cross_build 0
      shell: bash
    - name: cargo build openvmm
      run: |-
        flowey.exe e 5 flowey_lib_common::run_cargo_build 0
        flowey.exe e 5 flowey_lib_hvlite::run_cargo_build 0
        flowey.exe e 5 flowey_lib_hvlite::build_openvmm 0
        flowey.exe e 5 flowey_core::pipeline::artifact::publish 0
      shell: bash
    - name: 'validate cache entry: gh-release-download'
      run: flowey.exe e 5 flowey_lib_common::cache 3
      shell: bash
    - name: 🌼📦 Publish openvmm-aarch64-pc-windows-msvc-unsigned
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
      with:
        name: openvmm-aarch64-pc-windows-msvc-unsigned
        path: ${{ runner.temp }}/publish_artifacts/openvmm-aarch64-pc-windows-msvc-unsigned/
        include-hidden-files: true
  job6:
    name: draft openvmm release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    needs:
    - job0
    - job1
    - job2
    - job3
    - job4
    - job5
    if: github.event.pull_request.draft == false
    steps:
    - name: 🌼📦 Download artifacts
      uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
      with:
        pattern: '{_internal-flowey-bootstrap-x86_64-linux-uid-5,openvmm-aarch64-unknown-linux-musl,openvmm-vendor-release,openvmm-x86_64-unknown-linux-musl}'
        path: ${{ runner.temp }}/used_artifacts/
    - run: echo "${{ runner.temp }}/used_artifacts/_internal-flowey-bootstrap-x86_64-linux-uid-5" >> $GITHUB_PATH
      shell: bash
      name: 🌼📦 Add flowey to PATH
    - name: 🌼🛫 Initialize job
      run: |
        AgentTempDirNormal="${{ runner.temp }}"
        AgentTempDirNormal=$(echo "$AgentTempDirNormal" | sed -e 's|\\|\/|g' -e 's|^\([A-Za-z]\)\:/\(.*\)|/\L\1\E/\2|')
        echo "AgentTempDirNormal=$AgentTempDirNormal" >> $GITHUB_ENV

        chmod +x $AgentTempDirNormal/used_artifacts/_internal-flowey-bootstrap-x86_64-linux-uid-5/flowey

        echo '"debug"' | flowey v 6 'FLOWEY_LOG' update
        echo "${{ runner.temp }}/work" | flowey v 6 '_internal_WORKING_DIR' --is-raw-string update

        cat <<'EOF' | flowey v 6 'verbose' update
        ${{ inputs.verbose != '' && inputs.verbose || 'false' }}
        EOF
        echo "$AgentTempDirNormal/used_artifacts/openvmm-aarch64-unknown-linux-musl" | flowey v 6 'artifact_use_from_openvmm-aarch64-unknown-linux-musl' --is-raw-string update
        echo "$AgentTempDirNormal/used_artifacts/openvmm-vendor-release" | flowey v 6 'artifact_use_from_openvmm-vendor-release' --is-raw-string update
        echo "$AgentTempDirNormal/used_artifacts/openvmm-x86_64-unknown-linux-musl" | flowey v 6 'artifact_use_from_openvmm-x86_64-unknown-linux-musl' --is-raw-string update
      shell: bash
    - name: resolve release identity
      run: |-
        flowey e 6 flowey_core::pipeline::artifact::resolve 1
        flowey e 6 flowey_lib_hvlite::_jobs::publish_openvmm_gh_release 1
        flowey e 6 flowey_lib_hvlite::_jobs::publish_openvmm_gh_release 3
      shell: bash
    - name: create gh cache dir
      run: flowey e 6 flowey_lib_common::download_gh_cli 0
      shell: bash
    - name: Pre-processing cache vars
      run: |-
        flowey e 6 flowey_lib_common::cache 0
        flowey v 6 'flowey_lib_common::cache:2:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar1
        flowey v 6 'flowey_lib_common::cache:1:flowey_lib_common/src/cache.rs' --is-raw-string write-to-env github floweyvar2
      shell: bash
    - id: flowey_lib_common__cache__1
      uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25
      with:
        key: ${{ env.floweyvar1 }}
        path: ${{ env.floweyvar2 }}
      name: 'Restore cache: gh-cli'
    - name: installing gh
      run: |-
        flowey v 6 'flowey_lib_common::cache:4:flowey_lib_common/src/cache.rs' --is-raw-string update --env-source steps.flowey_lib_common__cache__1.outputs.cache-hit <<EOF
        ${{ steps.flowey_lib_common__cache__1.outputs.cache-hit }}
        EOF
        flowey e 6 flowey_lib_common::cache 2
        flowey e 6 flowey_lib_common::download_gh_cli 1
        flowey v 6 'flowey_lib_hvlite::_jobs::cfg_common:0:flowey_core/src/node/github_context.rs' --is-raw-string update --env-source github.token <<EOF
        ${{ github.token }}
        EOF
      shell: bash
    - name: setup gh cli
      run: flowey e 6 flowey_lib_common::use_gh_cli 0
      shell: bash
    - name: ensure no existing release
      run: |-
        flowey e 6 flowey_lib_hvlite::_jobs::publish_openvmm_gh_release 5
        flowey e 6 flowey_core::pipeline::artifact::resolve 2
        flowey e 6 flowey_core::pipeline::artifact::resolve 0
      shell: bash
    - name: enumerate release files
      run: |-
        flowey e 6 flowey_lib_hvlite::_jobs::publish_openvmm_gh_release 0
        flowey e 6 flowey_lib_hvlite::_jobs::publish_openvmm_gh_release 2
      shell: bash
    - name: pin release tag
      run: |-
        flowey e 6 flowey_lib_hvlite::_jobs::publish_openvmm_gh_release 6
        flowey e 6 flowey_lib_hvlite::_jobs::publish_openvmm_gh_release 4
      shell: bash
    - name: publish github releases
      run: flowey e 6 flowey_lib_common::publish_gh_release 0
      shell: bash
    - name: 'validate cache entry: gh-cli'
      run: flowey e 6 flowey_lib_common::cache 3
      shell: bash
