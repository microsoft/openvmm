# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

[package]
name = "tpm_lib"
rust-version.workspace = true
edition.workspace = true

[features]
# Use this feature to enable tests that pulls in the `ms-tpm-20-ref`
# with dependency on openssl.
test_tpm = ["ms-tpm-20-ref"]

[dependencies]
ms-tpm-20-ref = { optional = true, workspace = true }
tpm_protocol.workspace = true
cvm_tracing.workspace = true
inspect.workspace = true

thiserror.workspace = true
tracelimit.workspace = true
tracing.workspace = true
zerocopy.workspace = true

[target.'cfg(not(target_env = "musl"))'.dependencies]
ms-tpm-20-ref = { optional = true, workspace = true, features = ["vendored"] }

[dev-dependencies]
getrandom.workspace = true

[lints]
workspace = true
